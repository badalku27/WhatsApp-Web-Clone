[build]
  command = "npm run build"
  publish = "dist"

[dev]
  command = "npm run dev"
  port = 5173
  targetPort = 5173

[headers]
  # Allow the frontend to call the backend
  [[headers.values]]
  Access-Control-Allow-Origin = "*"
