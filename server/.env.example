## Backend environment variables
MONGODB_URI=
# Optional database name (defaults to 'whatsapp')
MONGO_DB_NAME=whatsapp
# Port for local dev; platforms will inject their own
PORT=8080
# Allowed frontend origins (comma separated). For local dev:
CORS_ORIGIN=http://localhost:5173
